/**
 * This file was auto-generated by Fern from our API Definition.
 */
import * as Vapi from "../index.mjs";
export interface FallbackCartesiaVoice {
    /** This is the flag to toggle voice caching for the assistant. */
    cachingEnabled?: boolean;
    /** This is the voice provider that will be used. */
    provider: "cartesia";
    /** The ID of the particular voice you want to use. */
    voiceId: string;
    /** This is the model that will be used. This is optional and will default to the correct model for the voiceId. */
    model?: FallbackCartesiaVoice.Model;
    /** This is the language that will be used. This is optional and will default to the correct language for the voiceId. */
    language?: FallbackCartesiaVoice.Language;
    /** Experimental controls for Cartesia voice generation */
    experimentalControls?: Vapi.CartesiaExperimentalControls;
    /** This is the plan for chunking the model output before it is sent to the voice provider. */
    chunkPlan?: Vapi.ChunkPlan;
}
export declare namespace FallbackCartesiaVoice {
    /**
     * This is the model that will be used. This is optional and will default to the correct model for the voiceId.
     */
    type Model = "sonic-2" | "sonic-english" | "sonic-multilingual" | "sonic-preview" | "sonic";
    const Model: {
        readonly Sonic2: "sonic-2";
        readonly SonicEnglish: "sonic-english";
        readonly SonicMultilingual: "sonic-multilingual";
        readonly SonicPreview: "sonic-preview";
        readonly Sonic: "sonic";
    };
    /**
     * This is the language that will be used. This is optional and will default to the correct language for the voiceId.
     */
    type Language = "en" | "de" | "es" | "fr" | "ja" | "pt" | "zh" | "hi" | "it" | "ko" | "nl" | "pl" | "ru" | "sv" | "tr";
    const Language: {
        readonly En: "en";
        readonly De: "de";
        readonly Es: "es";
        readonly Fr: "fr";
        readonly Ja: "ja";
        readonly Pt: "pt";
        readonly Zh: "zh";
        readonly Hi: "hi";
        readonly It: "it";
        readonly Ko: "ko";
        readonly Nl: "nl";
        readonly Pl: "pl";
        readonly Ru: "ru";
        readonly Sv: "sv";
        readonly Tr: "tr";
    };
}
